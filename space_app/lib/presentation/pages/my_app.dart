import 'package:flutter/material.dart';
import '../widgets/routes_widgets/routes_widget.dart';

class MyApp extends StatelessWidget {
  const MyApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => RoutesWidget();
}
